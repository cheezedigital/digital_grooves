
<h1> Index </h1>





<ul>
<% @items.each do |item| %>
  <li>
    <%= item.name %>
    <%= item.price %>
    <%= link_to('Destroy', item_path(item.id), method: :delete, 'data-confirm' => 'Are you sure?') %>
  </li>
  <% end %>
</ul>
